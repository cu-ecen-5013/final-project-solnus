<!DOCTYPE html>
<html>
<head>
    <style>
    h1 {text-align: center; 
        color: #0BECDF}
    body {
        background-color: #9C09E5;
        text-align: center;
    }
    .button {
        background-color: #4CAF50;
        color: white;
        padding: 3px 50px;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        font-size: 20px;
        margin: 4px 2px;
        cursor: pointer;
        border:2px solid #000;
    }
    .group {
        margin-bottom: 20px;
    }
    .group label {
        font-size: 22px;
        color:black;
        border:2px solid #000;
    }

    input[type="color"].withborder {
        -webkit-appearance: none;
        width: 180px;
        margin-top: 50px;
        position:relative;
        border:2px solid #000;
    }

    input[type="color"].withborder::-webkit-color-swatch {
        position:absolute;
        top:0px;
        left:0px;
        right:0px;
        bottom:0px;
        box-sizing: border-box;
        border:1px solid transparent;
    }
    </style>
</head>

<title>LED Control</title>
<h1> LED Controller</h1>
<body>
    <form action="/status" method="post">
        <div class="group">
            <label for="_cmd">Command</label>
            <input style="font-size:20px;" type="text" name="cmd" id = "_cmd" size = 15>
            <button type="submit" class="button">Send</button>
        </div>

    </form>
    
    <form action="/colorPick" method="post">
        <div class="group">
            <label for="clrpik">Pick color</label>
            <input type="color" class="withborder" name="clr" id="clrpik" value="#ff0000">
            <button type="submit" class="button">Send</button>
        </div>
    </form>

</html>
</body>

</html> 

